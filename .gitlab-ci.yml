image: node
cache:
  paths:
    - node_modules/

before_script:
  - npm install hexo@6.3.0 -g
  - npm install

pages:
  script:
    - hexo generate
  artifacts:
    paths:
      - public
  only:
    - master