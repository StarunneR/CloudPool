image: node
cache:
  paths:
    - node_modules/

before_script:
  - npm install hexo-cli -g --force
  - npm install
  - hexo version

pages:
  script:
    - hexo generate
  artifacts:
    paths:
      - public
  only:
    - master